<Window x:Class="MacroStudio.Presentation.Views.HotkeyCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Background="{DynamicResource Bg0}"
        Title="設置熱鍵"
        Width="500"
        Height="250"
        KeyDown="Window_KeyDown"
        PreviewKeyDown="Window_PreviewKeyDown"
        Focusable="True">
    <Border Style="{DynamicResource CardBorder}" Padding="20" Margin="14">
        <DockPanel LastChildFill="True">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                <TextBlock FontSize="18" FontWeight="SemiBold" Text="設置腳本熱鍵" Margin="0,0,0,8"/>
                <TextBlock Foreground="{DynamicResource TextMutedBrush}" Text="請按下您想要設置的按鍵或組合鍵" TextWrapping="Wrap"/>
            </StackPanel>

            <Border DockPanel.Dock="Top" 
                    Background="{DynamicResource Bg1}" 
                    BorderBrush="{DynamicResource BorderBrushSoft}"
                    BorderThickness="2"
                    CornerRadius="4"
                    Padding="16"
                    Margin="0,0,0,16"
                    MinHeight="80">
                <TextBlock x:Name="HotkeyDisplayText"
                           FontSize="24"
                           FontWeight="Bold"
                           Text="請按下按鍵..."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextBrush}"/>
            </Border>

            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Content="清除" Width="90" Margin="0,0,10,0" Click="ClearButton_Click" />
                <Button Content="取消" Width="90" Margin="0,0,10,0" Click="CancelButton_Click" />
                <Button Content="確認" Width="90" Style="{DynamicResource PrimaryButton}" Click="ConfirmButton_Click" IsEnabled="{Binding IsHotkeyValid}" />
            </StackPanel>
        </DockPanel>
    </Border>
</Window>
